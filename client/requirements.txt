httpx==0.27.2
pystray==0.19.5
Pillow>=10.4.0
python-dotenv==1.0.1
fastapi==0.115.0
uvicorn==0.30.6
# Transparent mode (Remote Control): WebSocket passthrough + local cert minting.
# websockets = upstream client channel; wsproto = uvicorn's server-side WS impl
# (kept separate so a websockets major bump can't break the server).
websockets>=12.0
wsproto>=1.2.0
cryptography>=41.0.0
